pipeline {
    agent any
    tools {
        maven "MAVEN"
        jdk "JDK"
    }
    stages {
        stage('Git clone'){
            steps{
                git branch: 'main', url: 'https://github.com/ChibuzoNE/jenkinsfile-testing.git'
            }
        }
         stage('build'){
            steps{
                echo "This code should better work o o"
            }
         }
}
